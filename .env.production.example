# ============================================================
# 排期录入系统 - 生产环境变量配置
# 使用方法：复制本文件为 .env，然后填写实际值
#   cp .env.production.example .env
# ============================================================

# ---------- 应用配置 ----------
# Flask 密钥（必填，用于 session 加密，请改为随机字符串）
SECRET_KEY=请改为一串随机字符串

# Gunicorn 工作进程数（默认2，小型服务器够用）
GUNICORN_WORKERS=2

# Gunicorn 每个进程的线程数（默认4）
GUNICORN_THREADS=4

# ---------- 端口配置 ----------
# Nginx 对外 HTTP 端口（默认80）
NGINX_HTTP_PORT=80

# Nginx 对外 HTTPS 端口（默认443）
NGINX_HTTPS_PORT=443

# ---------- 邮件配置（IMAP 收取 PO 邮件）----------
# IMAP 服务器地址（如 imap.exmail.qq.com）
IMAP_SERVER=

# IMAP 端口（默认993，SSL）
IMAP_PORT=993

# 邮箱账号
EMAIL_ACCOUNT=sales11@hanson2.com

# 邮箱密码（必填，明文存放，注意文件权限）
EMAIL_PASSWORD=

# ---------- 网络共享盘配置（SMB/CIFS）----------
# 排期文件所在的 SMB 共享路径（必填）
# 格式：//服务器IP/共享名/子路径
SMB_SHARE=//192.168.1.1/各客排期/ZURU生产排期

# SMB 登录用户名
SMB_USER=

# SMB 登录密码
SMB_PASSWORD=

# ---------- Docker 镜像配置 ----------
# Docker Registry 地址（留空则使用 Docker Hub）
REGISTRY=

# 镜像版本标签（默认 latest）
IMAGE_TAG=latest

# ---------- 部署配置（deploy.sh 使用）----------
# 远端服务器 IP 或域名（必填）
DEPLOY_SERVER=

# SSH 用户名（默认 root）
DEPLOY_USER=root

# SSH 端口（默认 22）
DEPLOY_SSH_PORT=22

# 远端项目部署路径
DEPLOY_PATH=/opt/schedule-system
